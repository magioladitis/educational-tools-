Matched runtime hotfix for ypologismos-didaktikon-anagkon.php.
Upload ALL files preserving folders, so the main PHP and its changed includes stay on the same version.
This package intentionally contains only the runtime files changed by the Gymnasium-with-Lyceum/atomic-allocation work.
PHP server-side code avoids PHP 7+ syntax such as ??, Throwable, arrow functions, typed properties and return/scalar type declarations.
