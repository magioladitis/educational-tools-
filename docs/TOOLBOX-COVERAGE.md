# Toolbox coverage after Batch 3

`ergaleia.php` contains 22 tool cards.

After the recent CSS-cleanup sequence:
- 19 of those 22 toolbox tools have been explicitly migrated/audited in the recent cleanup batches.
- `ypologismos-morion.php` and `dikaiologitika-tekna-anapiria.php` are already clean in the current stable regression/deploy copy (0 `<style>` blocks, 0 inline `style=`), so they do not need to be resent merely for CSS cleanup.
- The only toolbox tool not currently accounted for with a current CSS audit is:

  `dikaioma-symmetoxis.php`

The Library currently exposes only an older `dikaioma-symmetoxis-v3.2.php`, so the current server copy should be used before changing it.

`ergaleia.php` and `asep-tools.php` are hub/index pages and are additional to the 22 tool cards; both were cleaned in Batch 3.
