# SDE leadership module note

The uploaded `ypologismos-morion-diefthynton-ypodiefthynton-sde.php` references `includes/sde-leadership-calculations.js` without a cache-busting version.

For browser interaction testing of this CSS-only patch, the known Library artifact `sde-leadership-calculations-v3.15.js` was used under that expected filename. No calculation-module code is bundled or modified by this package.

This should be resolved/verified during the final 22/22 release audit so that the production unversioned module is proven to match the intended frozen version before the next stable snapshot.
