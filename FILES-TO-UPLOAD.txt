SDE LANGUAGE RUNTIME HOTFIX

Upload these 2 files preserving folders:
1. ypologismos-morion-apospasis-sde.php
2. includes/asep-language-selector.js

Why both files:
- asep-language-selector.js fixes readEntries() so it accepts a component id string, consistently with getState/reset/summary.
- ypologismos-morion-apospasis-sde.php bumps the selector cache-bust to v=3.20.27 so browsers do not keep the broken cached controller.

No CSS or scoring-engine change is required.
